/**
 * Stable side-effect entry point for the executor mock suite.
 *
 * Keep this filename extension-free at call sites so tests remain decoupled
 * from the implementation file's `.mock.ts` naming convention.
 */
import "./executor.mock";
