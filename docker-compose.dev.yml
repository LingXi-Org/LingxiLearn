# LingxiLearn local development.
#
# This is the only development Compose file. The browser and API read the
# current checkout through bind mounts; generated frontend assets are not
# copied into a production image in this mode.

name: lingxilearn-dev

x-api-dev: &api-dev
  build:
    context: ./server
    dockerfile: Dockerfile
    args:
      DOCKER_REGISTRY: ${DOCKER_REGISTRY:-accel.way2api.fun/docker.io}
      APT_MIRROR: ${APT_MIRROR:-mirrors.aliyun.com}
      PYPI_INDEX_URL: ${PYPI_INDEX_URL:-https://pypi.tuna.tsinghua.edu.cn/simple}
  env_file: [.env]
  environment:
    LINGXILEARN_DATABASE_URL: >-
      postgresql+asyncpg://${POSTGRES_USER:?set POSTGRES_USER}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:?set POSTGRES_DB}
    LINGXILEARN_PACKS_DIR: /app/packs
    LINGXILEARN_SKILLS_DIR: /app/skills
    LINGXILEARN_VAR_DIR: /app/var
    LINGXILEARN_AGENT_TASK_DIR: /app/var/agent_tasks
    LINGXILEARN_WEB_DIST: /app/web-dist-disabled
    LINGXILEARN_CORS_ORIGINS: '["http://localhost:3000","http://127.0.0.1:3000"]'
    LINGXILEARN_IDENTITY_BFF_URL: ${LINGXILEARN_IDENTITY_BFF_URL:-http://host.docker.internal:8081}
    LINGXILEARN_IDENTITY_BFF_TIMEOUT: ${LINGXILEARN_IDENTITY_BFF_TIMEOUT:-10}
    # Development intentionally uses the local learner identity. Keep this
    # explicit so a stale production-style .env cannot reintroduce 401s.
    LINGXILEARN_INSECURE_DEV_AUTH: "true"
  volumes:
    - ./server/lingxilearn:/app/lingxilearn
    - ./server/migrations:/app/migrations:ro
    - ./server/alembic.ini:/app/alembic.ini:ro
    - ./packs:/app/packs:ro
    - ./skills:/app/skills:ro
    - ./var:/app/var
  depends_on:
    postgres:
      condition: service_healthy
  security_opt: [no-new-privileges:true]

services:
  postgres:
    image: ${DOCKER_REGISTRY:-accel.way2api.fun/docker.io}/library/postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:?set POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER:?set POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
    volumes:
      - lingxilearn-dev-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 30
    restart: unless-stopped

  migrate:
    <<: *api-dev
    user: "0:0"
    command: ["alembic", "upgrade", "head"]
    restart: "no"

  api:
    <<: *api-dev
    user: "0:0"
    command:
      [
        "uvicorn",
        "lingxilearn.main:app",
        "--host",
        "0.0.0.0",
        "--port",
        "8000",
        "--reload",
        "--reload-dir",
        "/app/lingxilearn"
      ]
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    ports:
      - "${LINGXILEARN_PORT:-8080}:8000"
    restart: unless-stopped

  scheduler:
    <<: *api-dev
    user: "0:0"
    command: ["python", "-m", "lingxilearn.scheduler_worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  web:
    build:
      context: ./web
      dockerfile: docker/dev.Dockerfile
      args:
        DOCKER_REGISTRY: ${DOCKER_REGISTRY:-accel.way2api.fun/docker.io}
        NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmmirror.com}
    working_dir: /app
    # Keep shared runtime variables available to the frontend shell. Domain
    # reads and writes still go through the canonical FastAPI service.
    env_file: [.env]
    command:
      [
        "sh",
        "-c",
        "bun install --ignore-scripts --registry $${NPM_REGISTRY:-https://registry.npmmirror.com} && exec bun run dev"
      ]
    volumes:
      - ./web:/app
      - lingxilearn-dev-web-node-modules:/app/node_modules
      - lingxilearn-dev-web-next:/app/.next
    environment:
      NODE_ENV: development
      DATABASE_URL: >-
        postgresql://${POSTGRES_USER:?set POSTGRES_USER}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:?set POSTGRES_DB}
      NEXT_STATIC_EXPORT: "0"
      NEXT_PUBLIC_API_BASE: http://localhost:${LINGXILEARN_PORT:-8080}
      # The API container uses the same local development identity. Keep the
      # Next workspace shell in the same mode or it will probe Better Auth and
      # produce a stream of 401s before the Lingxi adapter is mounted.
      DISABLE_AUTH: "true"
      NEXT_PUBLIC_DISABLE_AUTH: "true"
      NEXT_PUBLIC_MOCK_AUTH: "true"
      NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmmirror.com}
    depends_on:
      api:
        condition: service_started
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  lingxilearn-dev-postgres-data:
  lingxilearn-dev-web-node-modules:
  lingxilearn-dev-web-next:
